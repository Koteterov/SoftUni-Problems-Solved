<section id="details-info">
    <h1>Details</h1>
    <div class="coin-image">
        <img src="{{item.image}}">
    </div>

    <div class="coin-info">
        <div class="coin-text">
            <h1 id="name">{{item.name}}</h1>
            <h3 id="payment">Payment method: {{item.payment}}</h3>
            <p id="price"><span>Price: ${{item.price}}</span></p>
            <p id="description">{{item.description}}</p>
        </div>

        {{#if user}}
        <div class="product-btn">
            {{!-- Only for registered user and author of the publication --}}

            {{#if isCreator}}
            <div class="author">
                <a href="/items/edit/{{item._id}}" class="btn-edit">Edit</a>
                <a href="/items/delete/{{item._id}}" class="btn-delete">Delete</a>
            </div>
            {{else}}

            {{#unless hasBought}}
            {{!-- Logged in user who has not yet buy this crypto --}}
            <a href="/items/buy/{{item._id}}" class="btn-buy">Buy</a>
            {{/unless}}

            {{#if hasBought}}
            {{!-- Logged in user who has already buy this crypto --}}
            <p class="buy-message">You already bought these crypto coins.</p>
            {{/if}}
            {{/if}}
        </div>
        {{/if}}

    </div>
</section>